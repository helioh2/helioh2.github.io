#lang racket

;; Arquivo para definição das constantes do programa.


(require 2htdp/image)
;inclua outros pacotes ou arquivos necessários usando (require ...)

(provide (all-defined-out)) ;permite que outros arquivos importem deste

;; =================
;; Constantes:

(define LAR-CEN 600)
(define ALT-CEN 400)
(define VEL-COB 6)
(define TAM-COB 16)
(define CEN (empty-scene LAR-CEN ALT-CEN))
(define POS-X-COB-INI (/ LAR-CEN 2))
(define POS-Y-COB-INI (/ ALT-CEN 2))
(define POS-X-SCO 5)
(define POS-Y-SCO 5)
(define RAIO-BIS 8)
(define COR-BIS "gold")
(define COR-COB "green")
(define COR-CAL "forestgreen")
(define LIM-SUP 0)
(define LIM-ESQ 0)
(define LIM-INF ALT-CEN)
(define LIM-DIR LAR-CEN)
(define INC-PONTOS 10)
(define DIST-COMER 12)
